# 净室重写工作区（clean-room）

本目录**不属于 walimaker 项目**（已在仓库根 `.gitignore` 中排除），它承载一个**独立的新项目**：
从行为规格重新实现一个教学用 2D 游戏引擎。

## 为什么要净室

原 `Walimaker` 是瓦力工厂的教学模块，本仓库是在它基础上做的修改版。继续分发"修改版"意味着
在分发**派生作品**——换名字解决不了这个问题（版权跟着代码走，不跟名字走）。
所以新引擎必须**由规格产出**，而不是由原实现改写。

## 两条纪律

1. **规格与实现分离**
   - `spec/` 由「可以阅读原项目的人」撰写，只描述**行为应当如何**（接口、语义、不变量、边界情况）；
   - `src/` 的实现只依据 `spec/` 与 `acceptance/`——**实现阶段不打开原项目源码**。
   - 同一个人分饰两角时，顺序必须是：先把规格写到位 → 再开始实现；不得一边看原实现一边写新实现。

2. **不复制表达，可复用设计**
   - **禁止**：复制原项目的任何源文件、注释、文档文本、测试代码；不得"把原文件搬过来再改"；
     不得复用 `Walimaker` / 瓦力工厂 名号与品牌（避免商标与"看起来像官方发布"）。
   - **允许**：功能层面的设计——公共接口形状（类名 / 方法名 / 参数）、行为语义、错误提示的**意图**
     （文案重写）、数学公式与算法思想、第三方库（pygame / pymunk / pytmx）的正常使用。
     接口与思想通常属于版权保护之外，**实现代码必须是新写的**。

## 目录结构

```
spec/               行为规格（唯一实现依据）
acceptance/         行为验收基线（27 项观测 + 整帧像素哈希）
src/                新引擎实现（从 spec 写出来）
tests/              针对 spec 新写的测试
SPEC-QUESTIONS.md   实现阶段若必须回头查证原项目的行为，在此登记"查了什么、结论是什么"
```

## 留痕（这是"净室过程"的证据）

- 每个实现提交在信息里写明依据的规格小节，例如 `feat(physics): 固定步长与自适应子步（spec/03）`。
- 实现阶段每次回头查证原项目的行为，都要在 `SPEC-QUESTIONS.md` 记一行：**问题 → 结论**，
  只记行为结论，不贴原代码。
- 新仓库的 git 历史从零开始，只包含净室产物。
