# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.2] - 2026-09-18

三个 bug 都是拿课堂项目（植物大战僵尸）当验收用例跑出来的。

### 修复

- **在一个对象的 `update()` 里 `kill()` 掉排在它后面的对象会崩**
  （`ValueError: list.remove(x): x not in list`）：集合遍历用的是快照，排在前面的对象可能已经
  把排在后面的对象移出集合，而死对象的清理又直接调了列表的 `remove`。现在统一走带判断的
  `Group.remove()`。示例里"种下植物就崩"就是这个：植物在自己 `update()` 里销毁虚影，
  而虚影排在它后面。
- **动画在画面上是冻住的**：`frame` 索引在推进，显示的却一直是同一帧——换帧没有让变换缓存
  （重绘标记）失效。手动 `frame = n` 同样处理。
- **透明度 / 着色只在第一帧生效**：着色只挂在那一次性的"需要重绘"标记上，而缩放过的贴图
  每帧都会重算图像，于是第二帧起颜色与透明度就丢了。示例里"虚影没有半透明效果"就是这个。
  现在着色是"当前颜色状态"的函数，每次重算都重新贴。
- **缩放过的贴图每帧都被重新缩放**（无谓开销）：判断要不要重算时拿当前缩放和 1 比，
  任何非 1 的缩放都恒不相等，于是每帧都做一次 `pygame.transform.scale`。
  现在和"上次重算时用的缩放"比。（实测这条在示例里不是帧率瓶颈，属于白烧的 CPU，
  修它是为了符合 spec 04 §1.3 的缓存约定，不是为了提速。）

- **卡片的"两张图切换"会频闪**：课堂写法是每帧写一次 `frame`（可买 / 置灰两张图），
  而多帧贴图默认在自动播放，两者互相顶：卡片每隔约 4~5 帧闪一次灰图。
  现在**写 `frame` 视为手动接管**——自动播放暂停（与 `pause_anim()` 同义），
  想继续自动播放调用 `play_anim()`。写 `state`（状态机换状态）不受影响，照旧自动播。

- **点窗口的关闭按钮关不掉程序**：`while True: update()` 这种课堂写法不会自己判断退出，
  于是窗口关了进程还在跑。现在收到关窗请求后，下一次 `update()` 会先收尾（拆物理空间、
  关窗口）再抛 `SystemExit`，循环自然结束；想自己处理退出（例如弹确认框）可以在循环里
  看到 `should_quit()` 为真时用 `reset_quit()` 拦下。
- **`libpng warning: iCCP: known incorrect sRGB profile` 刷屏**：两个原因——实体贴图
  加载**绕过了**现成的资源缓存（每造一个对象就重新解码一次磁盘文件），而且素材里的 ICC
  配置本身有毛病。现在贴图一律走 `ResourceManager` 缓存（同一路径只解码一次），并在解码前
  于内存里过滤掉 PNG 的 `iCCP` 块（**不改动磁盘上的素材**）：课堂示例实测从刷屏降到 0 条。

### 变更

- `color` 属性支持 `(红, 绿, 蓝)`（只改颜色、保留当前透明度）与 `(红, 绿, 蓝, 透明度)`；
  元素个数不对时给可读的中英双语错误（以前写三元素会直接 `IndexError`）。
- `Entity.frame = n` / `Sprite.frame = n` 现在会**暂停该贴图的自动播放**（见上）。
  只想跳到某一帧继续播的写法是 `obj.frame = n` 后再 `obj.play_anim()`。

## [0.2.1] - 2026-09-18

### 新增

- `Entity.set_dt(...)`：设置每帧停留时间。帧序列写 `set_dt(0.1)`；状态机写 `set_dt("walk", 0.1)`
  只改那一个状态（`set_dt(0.1)` 改所有状态）。`dt` / `set_dt` 现在要求**正数秒**，
  写 0 或非数字会给可读错误（以前写 0 会让帧推进死循环）。

## [0.2.0] - 2026-09-18

### 新增

- `Entity.get_mouse_just_clicked()` / `Entity.get_mouse_just_released()`：
  鼠标**刚在对象上按下 / 松开**的那一帧为真（`get_mouse_clicked()` 是"按住期间一直为真"），
  适合"点一下按钮"这类交互。
- 动画播放控制：`play_anim()` / `pause_anim()` / `stop_anim()` / `is_anim_playing()`。
  新对象默认就在播放；`play_anim()` 从当前帧继续，`stop_anim()` 回到第 0 帧并暂停。
  暂停只冻结帧推进（状态机切换也一并冻结），不影响物理、位置或绘制。
- `Entity.distance(目标)`：到另一个对象（用它的 `pos`）或某个坐标的直线距离；
  支持 `distance(enemy)` / `distance(x, y)` / `distance((x, y))` / `distance(vec(x, y))`，只算数不移动。

### 变更

- `TextBox.write(文字)` 更名为 `TextBox.print(文字)`；`write()` 保留为等价兼容名。
  这是**破坏性更名**：0.1.1 及更早的代码仍可运行（`write()` 是等价兼容名），但新代码请用 `print()`。

## [0.1.1] - 2026-09-18

### 修复

- **0.1.0 的 wheel 缺少子包**：`physics/`、`worldmap/`、`ui/` 没有被打进发行包，
  装上去 `import pixelclass` 就会 `ModuleNotFoundError`。打包配置改为自动发现 `src/` 下的
  所有子包；同时给发布流程加了防呆（见下）。

### 变更

- 发布脚本的"全新环境安装测试"现在会**先断言导入到的是装好的包**（而不是本地 `src/`），
  并显式覆盖 pytest 的 `pythonpath` 设置——0.1.0 就是因为这两点被蒙过去才发出去的。
- 新增 `tests/test_packaging.py`：静态校验打包配置覆盖了 `src/pixelclass/` 下所有子包。

## [0.1.0] - 2026-09-18

首版实现。规格先行的净室实现：`docs/spec/` 是唯一实现依据，`acceptance/` 是行为验收基线。

### 新增

- **场景与主循环**：`Scene`（每个场景一个物理空间 / 相机 / 时钟）、固定步长 1/60 + 自适应子步防穿透、
  可注入时钟（同一段场景可复现）、`setup/update/done`。
- **对象模型**：`Entity` 与三个可选组件槽位（视觉 / 单体刚体 / 瓦片刚体组）、
  课堂预设 `Character` / `Wall` / `Sensor` / `Mouse`；缺失组件一律"一次性中英双语提示 + 中性值"。
- **物理**：圆 / 盒 / 多边形 / 瓦片组、质量与位置写入约定（"没变就不重排宽相"）、
  三种碰撞判定路径（刚体 > 瓦片组 > 贴图 mask）、销钉与弹簧约束。
- **视觉与动画**：单图 / 帧序列 / 状态机 / 整图四种策略、变换缓存（只改位置不重算图像）、
  图层顺序、文本框与对话框。
- **地图**：TMX 载入（坐标只换算一次）、对象层 → 实体（图块对象自动带贴图）、整图渲染。
- **输入 / 音频 / 资源**：鼠标与键盘事件状态、文本输入、实体级悬停与点击；
  背景音乐与音效（无音频设备时安全无操作）；图片缓存。
- **教学支持**：中英双语报错与全局异常钩子、中文字体查找、调试绘制、帧率跟踪、慢动作。
- **验收基线**：`acceptance/run.py` 对拍 27 项行为观测（其中 25 项与旧实现逐项一致，
  2 项与素材像素相关、在本引擎自己的夹具上冻结）。
